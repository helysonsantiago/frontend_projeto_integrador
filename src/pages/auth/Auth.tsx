import React from "react";
import { StyledContainerAuth } from "./styled-auth";
const AuthComponent : React.FC = () : React.ReactElement => {
    return <StyledContainerAuth>
        <button></button>
    </StyledContainerAuth>;
}
export default AuthComponent;